const drop = document.querySelector('.net');

drop.addEventListener('click', () => {
    drop.classList.toggle('active');
});

const drip = document.querySelector('.nett');

drip.addEventListener('click', () => {
    drip.classList.toggle('active');
});