const dep = document.querySelector('.dep');

dep.addEventListener('click', () => {
    dep.classList.toggle('active');
});

const depp = document.querySelector('.dep2');

depp.addEventListener('click', () => {
    depp.classList.toggle('active');
});

const dep2 = document.querySelector('.dep3');

dep2.addEventListener('click', () => {
    dep2.classList.toggle('active');
});

const dep3 = document.querySelector('.dep4');

dep3.addEventListener('click', () => {
    dep3.classList.toggle('active');
});